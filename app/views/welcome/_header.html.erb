<nav class="navbar navbar-expand-lg navbar-dark bg-dark"">
  <div class="container-fluid">
    <a class="navbar-brand">Viewing Party</a>
    <button class="navbar-toggler navbar-toggler-custom" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if current_user.present? %>
          <li class="nav-item" style="padding-right:10px; padding-top:2px;">
            <%= link_to "Dashboard", user_dashboard_path(current_user), class: "btn btn-sm btn-light button-bg btn-outline-dark" %>
          </li>
          <li class="nav-item" style="padding-right:10px; padding-top:2px;">
            <%= link_to "Sign Out", logout_path(current_user.id), method: :delete, class: "btn btn-sm btn-light button-bg btn-outline-dark" %>
          </li>
          <% else %>
          <li class="nav-item" style="padding-right:10px; padding-top:2px;">
            <%= link_to "Sign In", root_path, class: "btn btn-sm btn-light button-bg btn-outline-dark" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
